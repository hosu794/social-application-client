import { shallow } from "enzyme";

import React from "react";

import LoggedButton from "../_components/layout/LoggedButton";

describe("Test for the LoggedButton Component", () => {
  const wrapper = shallow(<LoggedButton />);
});
