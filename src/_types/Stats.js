import PropTypes from "prop-types";
import { Component } from "react";

export const IStats = (Component.PropTypes = {
  storiesLiked: PropTypes.number,
  storiesCreated: PropTypes.number,
  lovesOnCreatedStories: PropTypes.number,
});
